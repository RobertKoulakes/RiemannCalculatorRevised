import { useState, useEffect } from 'react';

const SampleGraph = () => {
  return(
    <div className='graph' style={{display: 'inline-block', width: '100px', height: '100px', background: 'black'}}>
    </div>
  );
};

export default SampleGraph;